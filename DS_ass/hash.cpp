#include "Headers/Headers.h"
using namespace std;
// code flow
// int main()
// {
//     LogManager l;
//     Hash_chain chain;
//     chain.AddBlock(l.getTimestamp(), "Login", "Haseeb", "Student", 1);
//     chain.AddBlock(l.getTimestamp(), "Create Student", "Nawaz", "Librarian", 0);
//     chain.AddBlock(l.getTimestamp(), "Delete Student", "Umer", "Librarian", 1);
//     chain.AddBlock(l.getTimestamp(), "Modify Book", "Umer", "Librarian", 1);
//     chain.AddBlock(l.getTimestamp(), "Create Book", "Umer", "Librarian", 0);
//     chain.AddBlock(l.getTimestamp(), "Delete Book", "Umer", "Librarian", 1);
//     chain.AddBlock(l.getTimestamp(), "Create Librarian", "Umer", "Admin", 0);
//     chain.AddBlock(l.getTimestamp(), "Delete Librarian", "Umer", "Admin", 1);
//     chain.AddBlock(l.getTimestamp(), "Create Admin", "Umer", "Admin", 1);
//     chain.AddBlock(l.getTimestamp(), "Delete Admin", "Umer", "Admin", 0);
//     chain.printList();
//     chain.injectDataInChain(7, "MERA NAAM CHOMU PATEL HAI!");
//     chain.RemoveBlock(10);
//     chain.printList();
//     chain.checkChainConsistency() ? cout << "Healthy Chain" << endl : cout << "UNHEALTHY CHAIN ERROR!!!" << endl;
//     chain.recalulcateChain();
//     chain.checkChainConsistency() ? cout << "Healthy Chain" << endl : cout << "UNHEALTHY CHAIN ERROR!!!" << endl;

//     // string input = "saif Ur Rehman";
//     // SHA1 checksum;
//     // checksum.update(input);
//     // const string hash = checksum.final();
//     // cout << "The SHA-1 of \"" << input << "\" is: " << hash << endl;
//     return 0;

//     return 0;
// }
